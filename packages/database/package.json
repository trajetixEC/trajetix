{
  "name": "@trajetix/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "generate": "prisma generate", "migrate:dev": "prisma migrate dev", "migrate:deploy": "prisma migrate deploy", "studio": "prisma studio", "build": "prisma generate", "lint": "prisma validate", "typecheck": "prisma validate", "test": "prisma validate" },
  "dependencies": { "@prisma/client": "^6.10.1" },
  "devDependencies": { "prisma": "^6.10.1" }
}
