"use client";
import { FormEvent, useState } from "react";
import { useParams } from "next/navigation";
export default function ResetPage(){const {token}=useParams<{token:string}>();const [message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch("/api/auth/password/reset",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,password:form.get("password")})});setMessage(response.ok?"Contraseña actualizada. Ya puedes iniciar sesión.":"El enlace no es válido o ha caducado.")}return <main className="simple-auth"><div><span>⌁</span><h1>Nueva contraseña</h1><p>Usa al menos 12 caracteres y evita contraseñas reutilizadas.</p><form onSubmit={submit}><label>Nueva contraseña<input name="password" type="password" minLength={12} required /></label><button className="auth-submit">Actualizar contraseña</button></form>{message&&<div className={message.startsWith("Contraseña")?"auth-success":"auth-error"}>{message}</div>}</div></main>}
